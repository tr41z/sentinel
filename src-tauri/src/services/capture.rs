use tauri::AppHandle;

use pnet::datalink::NetworkInterface;

#[allow(unused)]
use crate::services::packet_handler::handle_packet;

#[allow(unused)]
pub fn capture_packets(interface: NetworkInterface ,app_handle: AppHandle) {}
